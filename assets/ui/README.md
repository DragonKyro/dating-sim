# UI assets

Reserved for icons, hearts, badges, and other small chrome elements. Empty
for v1 — the affection meter and other HUD elements are pure CSS.

If you add icons later, keep them small (≤64 × 64), SVG preferred so they
scale crisply on high-DPI screens.
